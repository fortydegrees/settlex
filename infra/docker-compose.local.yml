services:
  postgres:
    image: postgres:16-bookworm
    environment:
      POSTGRES_DB: settlehex
      POSTGRES_USER: settlehex
      POSTGRES_PASSWORD: settlehex
    ports:
      - "55432:5432"
    volumes:
      - settlehex-postgres-local:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  settlehex-postgres-local:
