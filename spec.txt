I think spec is also essentially board State?
    like what i'll need to save the board state as to import/export
    e.g. tiles, numbers, robber location

at the moment, boardGen does:
uses react-hexgrid to generate a shape/board
then creates all tiles as blank
then for each tile, generate edges and nodes



spec needs a react-hexgrid like function to generate the shape of the board/tiles
and then we just iterate through all tiles/nodes

so we also need our classic spec of like, ports,



mapGenFunc - generates shape of map (e.g. hex, custom polygon)


placementMoves: [[placeSettle, placeRoad], [placeSettle, placeRoad]]
                e.g. for CK would be [[placeSettle, placeRoad], [placeCity, placeRoad]]


standard:
mapGenFunc - ("hexagon", 3)


custom:
mapGenFunc - [some polygon thing] TODO: work out map shape from board-editor

seafarers
mapGenFunc - [some polygon thing]